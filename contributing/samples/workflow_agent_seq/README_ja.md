# Workflow Agent サンプル - SequentialAgent

サンプルクエリ：

* Write a quicksort method in python.
* Write a python function to do bubble sort.

CLIで実行するには（`google-adk`をインストール後）：

* `uv run main.py`（または`python main.py`）

このフォルダーの`sample.output`ファイルでサンプル出力を確認してください。